import 'package:flutter/material.dart';

/// This class defines the color palette used throughout the application.
class AppColors {
  /// The primary color, a vibrant yellow.
  static const Color primary = Color(0xFFFDD017);
  /// The secondary color, a light green.
  static const Color secondary = Color(0xFF90EE90);
  /// The accent color, a light orange.
  static const Color accent = Color(0xFFFFA07A);
}